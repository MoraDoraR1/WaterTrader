{
  "name": "bada-sangin-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "verify": "node scripts/verify-smooth-rendering.mjs && node build.mjs && node build-artifact.mjs",
    "dev": "node build.mjs --watch & npx serve . -l 5173"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
